/**
 * productsData alias exporting defaultProducts and mock catalog data.
 */
import { products, type Product } from "./mockProducts";

export const defaultProducts: Product[] = products;
export { products };
export default products;
